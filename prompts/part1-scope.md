# Part 1 implementation scope

Build a small local travel application with Vue in `frontend/` and Python FastAPI in `backend/`. Search supplied hotel and trip CSV data by hotel name, join records with `hotel_id`, show matching available stays in a labeled table, and show a clear no-results message when there are no matches.

Keep the checkpoint read-only: do not add SQLite, booking, booking history, or CRUD features.
